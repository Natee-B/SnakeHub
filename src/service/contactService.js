const prisma = require("../config/prisma")

const contactService = {}

contactService.contact = async()=>{
 //await prisma. ยังไงต่อ ต้องเป็น create เอาข้อมูลที่ได้จาก contact us ใส่ในDatabase ใหม่ตังหากเลยไหม
}

module.exports = contactService